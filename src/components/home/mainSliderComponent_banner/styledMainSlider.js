import styled from "@emotion/styled";

export const MainSliderContainer = styled.div`
  .mySwiper {
    .swiper-button-next,
    .swiper-button-prev {
      color: var(--primary-white);
    }
  }
  .slide {
    cursor: pointer;
    max-height: 300px;
    overflow: hidden;
  }
`;
